#create a more advanced executable using rules and variables


CC = gcc
SRC = main.c school.c
OBJ = ${SRC:%.c=%.o}
NAME = school
RM = rm -f

all: ${OBJ}
	${CC} ${OBJ} -o ${NAME}

clean:
	${RM} "~ ${NAME}

oclean:
	${RM} ${NAME}

fclean: clean oclean

re: fclean all
